
plugins {
  id "maven-publish"
}

group = "com.temp.bar"

def distFile = file("${projectDir}/dist.txt")

publishing {
  publications {
    main(MavenPublication) {
      artifact(distFile)
    }
  }

  repositories {
    maven {
      url "${projectDir}/../mavenTest"
    }
  }
}

